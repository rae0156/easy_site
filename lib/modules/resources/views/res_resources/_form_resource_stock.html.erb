	<%if (@object.stockable||'N')=='Y'%>
		<%= easy_tag('decimal', 	:instance => "object", :field => "qty_not_used", :mandatory => false, :with_label => "Quantité restante".trn, :bootstrap_form => true) %>
	<%end%>
