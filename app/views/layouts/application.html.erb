<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "EasySite #{@title.blank? ? '' : (' - ' + @title)}" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    
    <%unless @es_template_name.blank?%>
	    <%= stylesheet_link_tag "themes/#{@es_theme_name}", :media => "all" %>
	<%end%>
	<!-- File Tree -->
	<%= stylesheet_link_tag 'jqueryFileTree.css' %>		
		
	<!-- favicon(s) -->
	<link href="/favicon.ico" rel="shortcut icon">
	<link href="/favicon.ico" type="image/x-icon" rel="icon">
	<link href="/favicon.png" type="image/png" rel="icon">

    <%= javascript_include_tag "application" %>
  </head>
  <body>
	<%= render "layouts/part_message" %>
	<%= render "layouts/part_login" %>
	
    <%unless @es_template_name.blank?%>
		<%= render "templates/#{@es_template_name}" %>
	<%else%>
		<%= yield %>
	<%end%>
  </body>
</html>
