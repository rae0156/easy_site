    <div class="navbar navbar-fluid-top">
        <div class="container-fluid pull-left">
		  <%= label_tag('site_id', 'Sélectionner un site : ') %>
          <%= collection_select(:site, :id, EsSite.order("code"), :id, "complete_name", {:selected => Rails.application.config.default_site},{}) %>
        </div>
    </div>


<script>
	$(document).ready(function() {
		$('#site_id').on("change",function(e) {  
		    var valuesToSubmit = $('#site_id').serialize();
			$.ajax({
	        url: '/es_sites/change_default_site',
	        data: valuesToSubmit,
	        dataType: 'script',
		    });	    
		    return false;
		}
		
		);	
	});
</script>
